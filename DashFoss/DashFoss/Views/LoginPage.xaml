<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="DashFoss.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">

            <Label Text="Consumer Key:"/>
            <Entry Text="{Binding ConsumerKey}"/>

            <Label Text="Consumer Secret:"/>
            <Entry Text="{Binding ConsumerSecret}"/>

            <Label Text="Oauth Token:"/>
            <Entry Text="{Binding OauthToken}"/>

            <Label Text="Oauth Secret:"/>
            <Entry Text="{Binding OauthSecret}"/>

            <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>